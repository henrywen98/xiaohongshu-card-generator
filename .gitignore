# dependencies (run `npm install` in scripts/ to restore)
node_modules/

# macOS
.DS_Store

# logs
*.log

# generated cards (HTML is an intermediate artifact, PNG is output)
xhs_card_*.html
xhs_card_*.png
xhs_long_*.html
xhs_long_*.png
小红书文案.md

# local demo / scratch output (never commit generated samples)
demo/

# trigger eval 中间产物（trigger-eval.json + run-trigger-eval.sh 保留）
evals/results/
evals/log.txt
